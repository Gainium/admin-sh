# 001 — Wire mocha and CI to a repo with no tests

## Build order

1. Add devDependencies: `mocha`, `chai`, `@types/mocha`, `@types/chai`.
2. Add `test/placeholder.spec.ts` — one assertion, explicit "no real
   coverage yet" comment.
3. Add npm `"test"` script: mocha over `test/**/*.spec.ts` via
   `ts-node/register` (already a devDependency here).
4. Add `.github/workflows/test.yml`: PR + push to `main` (this repo's
   default), Node 22, `npm ci`, `npm test`. Runs alongside the existing
   `dco.yml`, doesn't replace it.
5. Verify `npm test` green locally.

## Blast radius

1. Other call sites: none — new file.
2. `core/` submodule: this repo has no `.gitmodules`.
3. CLAUDE.md contracts: none touched.
4. Other tests: none exist yet.

## Traceability

| Spec section | Test / file |
|---|---|
| §2.1/§2.3 | `test/placeholder.spec.ts`, `package.json` `"test"` |
| §2.2 | `.github/workflows/test.yml` |
