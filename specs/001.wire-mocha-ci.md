# 001 — Wire mocha and CI to a repo with no tests

## §1. Behaviour today

No test runner, no test files, `.github/workflows/dco.yml` exists but only
checks commit sign-off — nothing checks code correctness (§1.1).

## §2. Desired behaviour

`npm test` runs a real mocha suite and exits 0 (§2.1). CI runs `npm test`
on every PR (§2.2). One placeholder spec proves the wiring is real (an
explicit passing assertion, not a no-op) until actual coverage lands
(§2.3).

## §3. Open questions / assumptions

- §3.1 mocha + chai, per this task's brief — same pattern as the sibling
  "no tests yet" repos wired in this sweep (`admin-worker`, etc.).
  Pre-authorized, not left open.
